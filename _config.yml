theme: jekyll-theme-cayman

github:
  is_project_page: false

markdown: kramdown
kramdown:
  math_engine: katex

defaults:
  - scope:
      path: ""
    values:
      layout: default

baseurl: /rc/demos_companion-pages/cifka-ss-vq-vae

exclude:
  - node_modules
  - vendor
  - Gemfile
  - Gemfile.lock
  - package.json
  - yarn.lock
  - yarn-error.log
